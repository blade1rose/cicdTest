spring:
  datasource:
    url: jdbc:mysql://192.168.157.131:3306/studentManager?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: z.336612
    driver-class-name: com.mysql.cj.jdbc.Driver
# Prometheus springboot监控配置
management:
  endpoint:
    web:
      exposure:
        include: '*'
  metrics:
    tags:
      application: ${spring.application.name}
  prometheus:
    metrics:
      export:
        enabled: true

logging:
  level:
    root: info
